  sudo: required
  language: generic
  services:
    - docker

  before_install:
    - docker build -t docker007guru/docker-react -f Dockerfile.dev .

  script:
    - docker run -e CI=true docker007guru/docker-react npm run test
